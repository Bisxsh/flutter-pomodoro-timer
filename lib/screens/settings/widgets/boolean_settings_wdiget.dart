import 'package:flutter/material.dart';

class BooleanSettingsWidget extends StatelessWidget {
  const BooleanSettingsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
