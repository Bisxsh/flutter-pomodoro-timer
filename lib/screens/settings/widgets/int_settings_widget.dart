import 'package:flutter/material.dart';

class IntSettingsWidget extends StatelessWidget {
  const IntSettingsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
